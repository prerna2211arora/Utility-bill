Created Frontend APP Using: npm create vite@latest frontend (React
Javascript)
To Install packages:
npm install axios react-router-dom react-dropzone react-toastify

For Backend Setup:
mkdir backend
cd backend

npm init -y

npm install express mongoose dotenv cors jsonwebtoken bcryptjs multer
npm install bullmq ioredis langchain @langchain/community pdf-parse axios uuid
npm install -D nodemon

.\venv\Scripts\Activate.ps1